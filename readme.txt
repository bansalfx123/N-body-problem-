Import the GUI in Java, any IDE would work. 
Import the microcontroller code to the code composer studio.
The input will be given in a file called input.txt
Run the code composer studio code.
Run the java in your IDE.
In visualization mode, the planets motion will be displayed in the GUI.
In the compuataional mode, the output will also be written in the same folder in output.txt.

